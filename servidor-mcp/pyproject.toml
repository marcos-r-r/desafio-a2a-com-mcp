[project]
name = "servidor-mcp"
version = "1.0.0"
description = "Central de Salas: servidor MCP (Streamable HTTP, revisao 2026-07-28) com MRTR na reserva"
requires-python = ">=3.10"
dependencies = [
    "mcp==2.2.0",
    "mcp-types==2.2.0",
    "pydantic==2.13.5",
    "starlette==1.6.0",
    "uvicorn==0.53.0",
]

[tool.uv]
package = false
