[project]
name = "agente"
version = "1.0.0"
description = "Central de Salas: agente A2A v1.0 (JSON-RPC) que e host MCP por dentro"
requires-python = ">=3.10"
dependencies = [
    "a2a-sdk[http-server]==1.1.2",
    "httpx==0.28.1",
    "protobuf==6.33.6",
    "starlette==1.6.0",
    "uvicorn==0.53.0",
]

[tool.uv]
package = false
